extends base
include mixins/video

block content 
    each videoSample in videos
        +video(videoSample)
    else  
        li Sorry nothing found.

    //-통상적으로 each 단수 in 복수 이렇게 쓴다
    //-array 를 받아와 각각 리스트를 만들어준다