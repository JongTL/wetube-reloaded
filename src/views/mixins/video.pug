mixin video(video)
    div
            h4
                a(href=`/videos/${video.id}`)=video.title 
                //attribute에는 #{} 처럼 쓸 수 없다
            p=video.description
            small=video.createdAt
            hr